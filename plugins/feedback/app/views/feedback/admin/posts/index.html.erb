<%= t("All feedback") %> :

<%= form_for "", url: admin_posts_path, method: :get, class: "feedback--filter-form" do |f| %>
    <%= select_tag :status, options_for_select(Feedback::Post.statuses.map {[_1.first, _1.first] }, params[:status]), include_blank: true %>
    <%= f.submit "Filter", class: "feedback--button" %>
<% end %>

<% @posts.each do |post| %>
    <%= render "post", post: %>
<% end %>

<%= link_to(t("Back"), feedback_path) %>
